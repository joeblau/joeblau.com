name: Production

on:
  push:
    branches: [main]

jobs:
  deploy-production:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        worker: [jb-www, jb-travel, jb-gear]
    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.0"

      - name: Install dependencies
        run: bun install

      - name: Deploy production
        uses: cloudflare/wrangler-action@v3
        with:
          packageManager: bun
          preCommands: bun run cf-build
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: workers/${{ matrix.worker }}
        env:
          NEXT_PUBLIC_MAPBOX_TOKEN: ${{ matrix.worker == 'jb-travel' && secrets.NEXT_PUBLIC_MAPBOX_TOKEN || '' }}
